interface:
  display_name: "Chisel Internal Review"
  short_description: "Run supervised snapshot-pinned AI peer review"
  default_prompt: "Use $chisel-internal-review to run an Internal AI-persona review on a Chisel snapshot."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
