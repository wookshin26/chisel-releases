interface:
  display_name: "Chisel Review"
  short_description: "External referee cycle and internal persona review with parked approvals"
  default_prompt: "Use $chisel-review to run Chisel review work — import external referee reports with human attestation, draft the plan, point responses, response letter, and referee diff, or run internal AI-persona review against an immutable snapshot."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
