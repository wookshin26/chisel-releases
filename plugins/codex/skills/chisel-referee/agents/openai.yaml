interface:
  display_name: "Chisel Referee Assignments"
  short_description: "Assignment metadata and in-window AI-draft request relay"
  default_prompt: "Use $chisel-referee to check Monolith review assignments and relay an AI-draft request into the open assignment window."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
