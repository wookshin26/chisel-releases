interface:
  display_name: "Chisel Referee Assignments"
  short_description: "Assignment metadata, granted manuscript reading, and in-window AI-draft relay"
  default_prompt: "Use $chisel-referee to check Monolith review assignments, request a human-granted manuscript read, and help draft the review while execution and submission stay in the assignment window."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
