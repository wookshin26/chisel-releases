interface:
  display_name: "Chisel Review Reply"
  short_description: "Draft point-by-point external review replies"
  default_prompt: "Use $chisel-review-reply to draft a response for the active external-review round in Chisel."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
