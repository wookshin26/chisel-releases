interface:
  display_name: "Chisel Experiment"
  short_description: "Run supervised local or remote numerics work"
  default_prompt: "Use $chisel-experiment to inspect and run a bounded numerical experiment through Chisel."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
