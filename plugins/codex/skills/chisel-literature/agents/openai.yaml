interface:
  display_name: "Chisel Literature"
  short_description: "Collect papers and manage bibliography batches"
  default_prompt: "Use $chisel-literature to collect an OA paper or update a Chisel bibliography safely."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
