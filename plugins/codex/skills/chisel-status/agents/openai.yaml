interface:
  display_name: "Chisel Status"
  short_description: "Read-only project, compile, and review status"
  default_prompt: "Use $chisel-status to summarize the projects currently open in Chisel."

dependencies:
  tools:
    - type: "mcp"
      value: "chisel"
      description: "Chisel desktop app approval-gated MCP server"
